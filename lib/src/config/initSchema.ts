import type { FormSchema } from '@/types'

export default {
  labelWidth: 150,
  labelAlign: 'right',
  scrollToError: true,
  size: 'default',
  initialValues: {},
  items: []
} satisfies FormSchema
