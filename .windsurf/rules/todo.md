---
trigger: always_on
---

1. 目前首要任务：重构所有输入类表单组件-配置表单。统一支持属性、校验、联动、布局、事件（未实现）。目前开发进度，已经放在config/inputAttrSchema.ts。目前已经给input组件引用，期望完善后再复用给所有组件。
2. 自增组件的联动教程文档
3. 支持可视化配置JS表达式联动
4. ai改造：准备在设计器多处注入ai能力，比如ai对话生成表单（已实现）、输入框ai生成文本，ai生成校验规则。所以ai接口不再组件内部实现了，改成由用户全局注入(app.use时注入)，组件只负责传入不同提示词来获取结果。
5. 扩展一个【样式编辑器】自定义组件。这个组件以表单的形式编辑各种css样式。这个表单生成的是一个style对象。
6. StyleEditor下扩展一个UnitInput 智能样式值输入组件，用于替换普通的css样式输入。这个组件默认是一个按钮，每次点击在几个值或单位之间切换，如果是单位，就显示数字输入框。
7. StyleEditor下基于UnitInput扩展一个 智能边距编辑组件，默认只展示统一边距，可以展开分别编辑四个方向的子边距，其中一个子边距变了，应该清掉父边距。反之，父边距改变应该联动改变4个子边距
